SELECT * FROM users;
-- selecciona todos los campos de la tabla users

SELECT name FROM users;
-- selecciona la columna name de la tabla users

SELECT user_id, name FROM users;
-- selecciona las columnas user_id y name de la tabla users