SELECT * FROM users WHERE name IN ('Oscar');
-- devuelve todos los campos cuyo valor de name sea Oscar.

SELECT * FROM users WHERE name IN ('Oscar', 'sara');
-- devuelve todos los campos cuyo valor de name sea oscar o sara